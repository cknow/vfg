export default {
    group: {
        enabled: true,
        tag: 'fieldset',
        prepend: {
            legend: {
                enabled: true,
                tag: 'legend'
            }
        }
    }
};
